<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>TryNoClue – AI Simplifier</title>

<style>
  :root {
    --bg: #0f0f0f;
    --card: rgba(255, 255, 255, 0.05);
    --text: #ffffff;
    --accent: #00ffa6;
  }

  body.light {
    --bg: #f5f5f5;
    --card: rgba(0,0,0,0.05);
    --text: #111;
    --accent: #00895c;
  }

  body {
    margin: 0;
    font-family: Arial, sans-serif;
    background: var(--bg);
    color: var(--text);
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    transition: background 0.3s ease, color 0.3s ease;
  }

  .container {
    background: var(--card);
    backdrop-filter: blur(12px);
    padding: 35px;
    width: 92%;
    max-width: 650px;
    border-radius: 16px;
    box-shadow: 0 0 40px rgba(0, 0, 0, 0.25);
    animation: fadeIn 0.6s ease forwards;
    opacity: 0;
  }

  @keyframes fadeIn {
    to { opacity: 1; }
  }

  .logo {
    text-align: center;
    font-size: 34px;
    font-weight: bold;
    margin-bottom: 10px;
    color: var(--accent);
    letter-spacing: 1px;
  }

  .subtitle {
    text-align: center;
    margin-bottom: 25px;
    opacity: 0.8;
    font-size: 16px;
  }

  .toggle {
    position: absolute;
    top: 20px;
    right: 20px;
    background: var(--card);
    padding: 10px 14px;
    border-radius: 10px;
    cursor: pointer;
    font-size: 14px;
    color: var(--text);
    border: 1px solid var(--accent);
    transition: 0.2s;
  }
  .toggle:hover {
    background: var(--accent);
    color: #000;
  }

  textarea {
    width: 100%;
    min-height: 130px;
    padding: 14px;
    border-radius: 10px;
    border: none;
    font-size: 16px;
    resize: vertical;
    background: rgba(255,255,255,0.08);
    color: var(--text);
    outline: none;
    transition: 0.2s;
  }
  textarea:focus {
    box-shadow: 0 0 10px var(--accent);
  }

  button {
    width: 100%;
    padding: 15px;
    border: none;
    border-radius: 12px;
    margin-top: 18px;
    background: var(--accent);
    color: #000;
    font-size: 20px;
    font-weight: bold;
    cursor: pointer;
    transition: 0.25s ease;
  }
  button:hover {
    transform: translateY(-2px) scale(1.01);
    box-shadow: 0 0 15px var(--accent);
  }

  .output-box {
    margin-top: 25px;
    padding: 22px;
    background: rgba(255,255,255,0.07);
    border-radius: 10px;
    min-height: 80px;
    font-size: 16px;
    white-space: pre-wrap;
    line-height: 1.5;
    opacity: 0;
    transform: translateY(10px);
    transition: 0.3s ease;
  }
  .output-visible {
    opacity: 1 !important;
    transform: translateY(0px);
  }

  .loader {
    width: 28px;
    height: 28px;
    border: 4px solid #fff;
    border-top: 4px solid var(--accent);
    border-radius: 50%;
    margin: 18px auto;
    animation: spin 0.8s linear infinite;
  }
  @keyframes spin {
    to { transform: rotate(360deg); }
  }
</style>
</head>

<body>

<div class="toggle" onclick="toggleTheme()">🌗 Theme</div>

<div class="container">
  <div class="logo">TryNoClue</div>
  <div class="subtitle">AI Text Simplifier</div>

  <textarea id="userText" placeholder="Paste your text here..."></textarea>
  <button onclick="simplify()">Simplify</button>

  <div id="output" class="output-box"></div>
</div>

<script>
  function toggleTheme() {
    document.body.classList.toggle("light");
  }

  async function simplify() {
    const text = document.getElementById("userText").value;
    const output = document.getElementById("output");
    
    if (!text.trim()) {
      output.innerHTML = "Please enter text first.";
      output.classList.add("output-visible");
      return;
    }

    output.innerHTML = "<div class='loader'></div>";
    output.classList.add("output-visible");

    try {
      const response = await fetch("/api/simplify", {
        method: "POST",
        headers: {"Content-Type": "application/json"},
        body: JSON.stringify({ text })
      });

      const data = await response.json();

      output.innerHTML = data.message || "No response.";
    } catch (err) {
      output.innerHTML = "Error contacting AI.";
    }
  }
</script>

</body>
</html>
